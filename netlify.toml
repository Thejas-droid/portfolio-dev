[build]
  command = "npm run build"
  publish = "dist/client"

# Fallback redirect for single-page app (SPA) / client routing on static CDN
[[redirects]]
  from = "/*"
  to = "/_shell.html"
  status = 200
